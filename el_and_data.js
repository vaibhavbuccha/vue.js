// we create a instance of Vue class.
var vue = new Vue({
    // el is predefine which indicates element in which we pass our id on which we want controll.
    el : "#our-app",
    // data is also predefine in which we have to pass our data .
    data :{
        message : "this is vue message",
        fname : "vaibhav buccha",
        from : "bikaner"
    }
});
